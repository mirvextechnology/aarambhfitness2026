/**
 * TESTIMONIALS
 * ------------------------------------------------------------
 * ⚠ IMPORTANT: Ships EMPTY on purpose.
 * We never fabricate customer names, photos or results.
 *
 * Add real, permission-given reviews using this shape and the
 * slider renders them automatically. While empty, the section
 * shows an honest "reviews coming soon" state instead.
 *
 * {
 *   id: 'review-01',
 *   name: 'Member name (as they wish to appear)',
 *   goal: 'Fat Loss',
 *   result: 'Optional — only if the member confirmed it',
 *   quote: 'The actual words the member used.',
 *   image: importedImage,   // optional
 *   imageAlt: 'Photo of the member',
 *   date: '2026-03-14',      // optional
 * }
 * ------------------------------------------------------------
 */

export const testimonials = [
  // ⚠ EDIT ME — add genuine member reviews here.
];

export const hasTestimonials = testimonials.length > 0;

export default testimonials;
